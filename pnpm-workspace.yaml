packages:
  - "apps/*"
  - "packages/*"
  - "tools/*"

# Dependency install scripts are blocked by default.
# Each true entry is explicitly reviewed and allowed to execute lifecycle scripts.
allowBuilds:
  esbuild: true
